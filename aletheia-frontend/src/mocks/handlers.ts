/**
 * MSW handlers (re-export).
 *
 * The source of truth lives under `src/test/msw/handlers/` to keep test contracts
 * explicit and enforceable. This file exists to preserve existing imports.
 */
export { handlers } from '@/src/test/msw/handlers';

