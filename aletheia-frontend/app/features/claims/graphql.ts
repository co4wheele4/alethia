/**
 * Claims (inspection-only) GraphQL operations.
 *
 * This module re-exports the canonical fragment/query documents from `src/graphql`
 * to keep schema usage explicit and contract-validated.
 */
export { CLAIM_FIELDS, CLAIM_EVIDENCE_FIELDS, LIST_CLAIMS_QUERY, CLAIMS_BY_DOCUMENT_QUERY } from '@/src/graphql';

