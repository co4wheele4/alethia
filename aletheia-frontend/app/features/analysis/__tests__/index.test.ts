/**
 * Tests for ai index exports
 */

import * as analysis from '../components';

describe('analysis index', () => {
  it('should export analysis components', () => {
    expect(analysis).toBeDefined();
  });
});
