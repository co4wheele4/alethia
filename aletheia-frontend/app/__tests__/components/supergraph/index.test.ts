/**
 * Tests for supergraph index exports
 */

import * as supergraph from '../../../components/supergraph';

describe('supergraph index', () => {
  it('should export supergraph components', () => {
    expect(supergraph).toBeDefined();
  });
});
