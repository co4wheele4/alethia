/**
 * Tests for search index exports
 */

import * as search from '../../../components/search';

describe('search index', () => {
  it('should export search components', () => {
    expect(search).toBeDefined();
  });
});
